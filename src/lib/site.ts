/** 사이트 주인의 UID — 홈 화면의 바로가기와 기본 프로필에 쓴다. */
export const OWNER_UID = process.env.NEXT_PUBLIC_OWNER_UID ?? "800133616";

/** 사이트 주소 — 공유 카드·sitemap·robots 에서 쓴다 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://hsr-builds.vercel.app";
